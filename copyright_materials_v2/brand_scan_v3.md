# 穿戴甲AIGC商品视觉生产管理系统提交页扫描 V3

软件简称：穿戴甲视觉生产系统  
版本号：V1.0  
扫描对象：`source_submission_candidate.txt`

## 1. 扫描范围

- 前 30 页：候选第 1–1677 行。
- 后 30 页：候选第 10976–12475 行，保持 V2 不变。
- 前段终点：`TaskService.create_generation()` 的完整结束行。
- 后段起点：`inputs.py` 的完整 `load_all()` 函数定义。
- 后段终点：`db.py` 文件真实末行。

所有行号均为 `source_submission_candidate.txt` 的全局物理行号。

## 2. 前 30 页品牌扫描

| 扫描词 | 出现次数 | 全局行号 | 结论 |
|---|---:|---|---|
| `FINGLOW` | 0 | 无 | 达标 |
| `finglow` | 0 | 无 | 达标 |
| `api.finglow.cn` | 0 | 无 | 达标 |
| `Lunelle Studio` | 0 | 无 | 达标 |
| `Lunelle Nails` | 0 | 无 | 达标 |
| `lunelle`（大小写不敏感） | 8 | 1、155、215、405、495、768、978、1032 | 内部技术标识，需人工判断 |

前段 `lunelle` 明细：

- 第 1、215、495、768、978、1032 行：`# File: lunelle/...` 文件路径标记。
- 第 155 行：技术注释中的 `lunelle/urlguard.py` 包路径。
- 第 405 行：Schema 描述中的 `LUNELLE_CONFIRM_COST_USD` 环境变量名。

## 3. 后 30 页品牌扫描

| 扫描词 | 出现次数 | 全局行号 | 结论 |
|---|---:|---|---|
| `FINGLOW` | 0 | 无 | 达标 |
| `finglow` | 0 | 无 | 达标 |
| `api.finglow.cn` | 0 | 无 | 达标 |
| `Lunelle Studio` | 0 | 无 | 达标 |
| `Lunelle Nails` | 0 | 无 | 达标 |
| `lunelle`（大小写不敏感） | 11 | 11153、11354、11536、11761、11822、12175、12277、12286、12338、12345、12374 | 内部技术标识，需人工判断 |

后段 `lunelle` 明细：

- 第 11153、11536、11761、11822、12175、12277 行：文件路径标记。
- 第 11354 行：技术注释中的 `lunelle/build.py` 包路径。
- 第 12286、12338、12345、12374 行：SQLite 迁移加载使用的 Python 包名、迁移目录或迁移控制标记。

这些位置均不是客户名称或客户域名。为了保持真实源码标识，没有重命名包、环境变量或迁移标记。

## 4. 客户部署适配隔离

客户项目历史部署/适配文字仍真实保留在完整候选中部，但不进入前后 30 页：

| 标识 | 完整候选行号 | 所在文件 | 是否进入提交页 |
|---|---:|---|---|
| `Lunelle Nails` | 7476 | `lunelle/export.py` | 否 |
| `api.finglow.cn` | 7858 | `lunelle/publish.py` | 否 |
| `Lunelle Studio` | 8135 | `lunelle/server.py` | 否 |

第 8118 行的小写日志文字 `lunelle studio started` 也位于候选中部，不进入前后 30 页。

## 5. 凭证和敏感字段扫描

| 检查项 | 前 30 页命中 | 后 30 页命中 | 审核结果 |
|---|---|---|---|
| API Key 字段词 | 第 467、480 行，共 2 次 | 第 11276 行 3 次；第 11285 行 1 次，共 4 次 | 只有字段、变量和 SHA-256 指纹，无值 |
| Token 字段词 | 第 423 行，共 1 次 | 0 | 空默认值字段，无 Token |
| Secret 字段词 | 0 | 第 10992、11273 行，共 2 次 | 叙述性技术说明，无 Secret |
| JWT 关键词/格式 | 0 | 0 | 未发现 |
| Private Key 关键词/PEM | 0 | 0 | 未发现 |
| Database URL/带密码连接串 | 0 | 0 | 未发现 |
| IPv4/IPv6 地址字面量 | 0 | 0 | 未发现 |

额外凭证格式扫描结果：OpenAI 风格长 Key、AWS Access Key、Google API Key、三段式 JWT、PEM 私钥、带用户名密码的数据库 URL、长格式 Bearer Token，前后两段全部为 0。

## 6. 客户数据和个人信息扫描

| 检查项 | 前 30 页 | 后 30 页 | 结论 |
|---|---:|---:|---|
| 真实客户数据记录 | 0 | 0 | 未发现客户名称、域名、SKU 记录、上传内容或数据库记录 |
| 电子邮箱 | 0 | 0 | 未发现 |
| 中国大陆手机号 | 0 | 0 | 未发现 |
| 中国身份证号 | 0 | 0 | 未发现 |
| 真实姓名/个人地址记录 | 0 | 0 | 未发现 |
| IP 地址值 | 0 | 0 | 未发现 |

语义复核说明：

- 第 492 行的 `old client` 表示旧版调用端，是兼容性注释，不指向任何客户。
- 第 156 行的 `internal address` 表示 SSRF 防护中的内部网络地址概念，不含地址值，也不是个人地址。
- `style_id`、`sku`、`profile_id` 等均为源码字段名，不是运行时客户记录。

## 7. 最终结论

V3 前 30 页和保持不变的后 30 页中，`FINGLOW`、`finglow`、`api.finglow.cn`、`Lunelle Studio`、`Lunelle Nails` 的命中数全部为 0。未发现真实 API Key、Token、Secret、JWT、Private Key、数据库连接凭证、IP 地址、客户业务数据或个人信息。

