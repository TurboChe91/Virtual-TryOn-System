# 穿戴甲AIGC商品视觉生产管理系统软件著作权申请材料总览

软件全称：穿戴甲AIGC商品视觉生产管理系统  
软件简称：穿戴甲视觉生产系统  
版本号：V1.0  
材料生成日期：2026-08-22

## 1. 当前项目是否已经适合申请软著

**结论：项目的真实源码完整度和自主业务逻辑已经足以整理软著材料，但目前属于“可进入人工核验与排版阶段”，不建议未经确认直接提交。**

支持申请的代码事实包括：

- 存在完整的款式/SKU、素材、Prompt、AI 任务、状态机、SQLite 队列、Worker、失败重试、输入快照、QA、人工审核、候选结果、统计、导出和 R2/D1 发布链路。
- 核心首方后端代码量充足；第二轮提交净化候选包含 12,327 行真实文件内容、12,475 个物理行，超过普通交存前后各 30 页的数量需要。
- 候选没有包含测试、Mock、第三方依赖源码、HTML/CSS 模板、`.env`、运行数据库或客户素材。
- 候选凭证特征扫描没有发现真实 API Key、AWS Key、JWT、私钥、带密码数据库 URL 或硬编码 Bearer Token。

提交前的主要阻碍不是代码数量，而是以下人工事项：

1. 源码内部仍使用 `lunelle` / `Lunelle Studio` 历史技术标识；`FINGLOW`、`api.finglow.cn` 和 `Lunelle Nails` 属于客户项目历史部署/适配标识，必须与本软件登记名称区分。
2. README 说明存在前身项目，需确认前身代码、贡献者、职务/委托/合作开发关系和著作权归属。
3. 著作权人身份、完成/发表日期、开发方式、运行环境等登记信息尚未提供。
4. 用户说明书截图尚未由开发者在安全演示环境中制作。
5. 完整候选中的 Prompt、品牌、域名和历史事件注释需要权利人最后审阅。

## 2. 已准备完成的材料

| 文件 | 状态 | 内容 |
|---|---|---|
| `01_project_audit.md` | 已完成 | 架构、技术栈、目录、模块、数据流、任务与 AI 工作流 |
| `02_source_code_selection.md` | 已完成 | 文件级行数、推荐性、敏感字段和第三方风险 |
| `03_source_candidate.txt` | 第一轮存档 | 49 个真实首方文件、12,871 行原始源码；不再作为最终排版来源 |
| `03_source_manifest.md` | 第一轮存档 | 记录第一轮来源、顺序和行号，已由第二轮计划替代 |
| `04_continuity_plan.md` | 第一轮存档 | 记录第一轮连续性设计，已由第二轮计划替代 |
| `05_software_information.md` | 已完成草案 | 软件基本信息、功能、技术特点、环境和待确认项 |
| `06_user_manual_draft.md` | 已完成草案 | 18 章用户操作说明及截图占位符 |
| `07_submission_risk_audit.md` | 已完成 | 安全、隐私、第三方、许可证、品牌和权属风险 |
| `README.md` | 已完成 | 当前总览和下一步清单 |
| `../copyright_materials_v2/source_submission_candidate.txt` | 第二轮当前版本 | 49 个真实首方文件的净化副本，客户部署适配文件置于中部 |
| `../copyright_materials_v2/brand_scan.md` | 第二轮当前版本 | 前后各 30 页品牌/历史技术标识逐项计数与行号 |
| `../copyright_materials_v2/final_deposit_plan.md` | 第二轮当前版本 | 最终顺序、总行数、前后 30 页边界、截断与敏感扫描 |

材料分别位于 `copyright_materials/` 和 `copyright_materials_v2/`。原始项目文件未被本次工作修改；生成前后对原有 132 个受审计文件计算的集合哈希一致。没有执行 Git 提交。

## 3. 还需要本人提供的信息

### 申请人/权利信息

- 著作权人法定姓名。
- 证件类型和号码、联系地址、邮编、电话和电子邮箱。
- 开发方式：独立开发、合作开发、委托开发或职务开发。
- 权利取得方式和权利范围。
- 如有合作方、雇主、委托方或其他贡献者，相应合同/声明和权属证明。

### 时间与发表信息

- 开发开始日期和开发完成日期。
- 是否已经发表。
- 首次发表日期、地点和方式（如适用）。
- V1.0 的实际完成/发布依据。

### 技术环境

- 实际开发操作系统、版本、开发工具/IDE、CPU 和内存。
- 实际生产运行操作系统、服务器配置、Python/SQLite 版本。
- 正式支持的浏览器及最低版本。
- 实际生产访问域名；不要在材料中提供内网地址或凭证。

### 名称与权属

- `lunelle` / `Lunelle Studio` 作为内部历史技术标识的说明。
- `FINGLOW`、`Lunelle Nails`、`api.finglow.cn` 作为客户项目历史部署/适配标识的隔离说明。
- 两个前身项目的著作权归属、是否本人独立创作、是否使用第三方/前雇主/客户代码。
- Prompt 模板、视图契约、手模槽位规则是否允许作为公开交存内容。

## 4. 需要手动截图的页面

建议在隔离的演示/测试环境中，用自有或虚构款式数据截图，不要使用真实客户资料、真实密钥、内网地址或生产错误日志。

优先截图：

1. 系统首页/款式库和顶部统计指标。
2. 新建款式——从描述创建。
3. 新建款式——按图识别（仅在安全演示 LLM 通道可用时）。
4. 款式卡片和款式管理弹窗。
5. 素材上传、自动切分结果、Contact Sheet。
6. 人工调整十甲切图页面。
7. 十甲身份编辑区。
8. 设置页的管理口令、生图通道、识别模型、Cloudflare 和手模矩阵；所有 Key/Token/ID 必须为空或遮盖。
9. 试戴矩阵费用估算/确认框。
10. 生成队列及状态筛选。
11. 成功任务详情、结果图和 QA。
12. 失败任务详情、错误代码、重试/取消操作。
13. 人工批准/拒绝区域。
14. 候选版本对比和候选选择。
15. 提交修正区域。
16. 导出素材包成功提示。
17. 发布就绪状态；若截图会触及生产配置，可以不执行真实发布，只截取已脱敏页面状态。

`06_user_manual_draft.md` 中已放置更细的 `【截图：……】` 占位符。截图完成后应统一窗口尺寸、浏览器缩放、标题和演示数据，并对任何敏感信息二次检查。

## 5. 需要人工审核的代码

### 最高优先级

- `lunelle/prompts.py`：完整生成规则可能属于商业秘密；确认是否全部交存。
- `lunelle/publish.py`、`lunelle/cloudflare.py`：包含公开域名、R2/D1 表名、对象 key 规则和发布架构。
- `lunelle/export.py`：含客户适配 Vendor 文本 `Lunelle Nails`，应置于完整候选中部并避开前后 30 页。
- `lunelle/config.py`、`lunelle/profiles.py`、`lunelle/schemas.py`：只有敏感字段名，没有值；人工确认后可以保留。
- `lunelle/build.py`、迁移 0012/0013 及其他长注释：含历史故障背景、日期、费用或历史数据量说明，确认是否属于内部信息。

### 其次

- 所有迁移：确认表名和业务字段不包含不宜公开的内部命名。
- `llm.py`：确认识别与 QA Prompt 不含客户特定规则。
- `server.py`：确认 API 路径和管理模式可以公开。
- `providers/__init__.py`：候选保留生产 Provider 构建入口，但按政策没有纳入 `providers/mock.py`；候选文本本身不以独立运行作为目标。

人工审核只应修改软著候选副本，不应为了申请去回改真实业务仓库。可以从候选副本删除纯历史/调试/TODO 注释，但不得改写逻辑、编造代码或靠注释补行数。

## 6. 是否发现第三方代码风险

**未发现第三方完整源码混入候选。** 没有纳入 `node_modules`、虚拟环境、SDK 源码、minified 文件或依赖包。

仍有待确认风险：

- 项目调用 FastAPI、Pydantic、Pillow、httpx 等开源库，需要在产品分发层面维护依赖许可证清单；这些库源码不应加入软著交存。
- 仓库根目录未发现 LICENSE/COPYING/NOTICE，不能据此证明所有首方代码权属。
- 前身项目与贡献者权属需要通过 Git 历史、劳动/委托/合作关系核验。
- 如果有从模板、教程、代码生成器或 AI 工具取得的大段实现，应由本人确认其授权和独创性；当前文件本身没有自动生成标记。

## 7. 是否发现敏感信息

**在第二轮 `../copyright_materials_v2/source_submission_candidate.txt` 中未发现真实凭证特征或真实个人信息。**

已确认但未读取、且必须保持排除：

- 仓库根目录存在 `.env`。
- 仓库存在 `data/` 运行数据目录。

这两个位置可能包含真实 API Key、管理员 Token、Cloudflare 配置、SQLite 数据库、客户素材、生成图、日志、导出和备份，绝不能加入申请包。

候选中存在正常的 `api_key`、`token`、`secret`、`password`、`R2`、`Cloudflare` 字段名和脱敏逻辑；它们不是秘密值。候选还包含品牌、公开域名、内部流程与历史注释，属于需要权利人判断的商业信息风险，而不是凭证泄漏。

## 8. 下一步应该做什么

1. **确认权属和名称。** 使用新的登记名称，说明 `lunelle` 内部技术标识，并将客户部署标识与本软件权利范围隔离；同时核对前身项目关系和申请人信息。
2. **人工审查完整候选。** 以第二轮 `source_submission_candidate.txt` 为当前版本，重点阅读 Prompt、发布、导出和迁移；只在候选副本中删除不宜公开的非功能注释。
3. **冻结最终源码顺序。** 使用 `../copyright_materials_v2/final_deposit_plan.md` 的顺序，不再随意重排。
4. **制作前后 30 页。** 从第二轮候选连续取物理行 1–1500 和 10976–12475，并依据登记机构当期排版规则复核；保留文件路径和页码。
5. **制作用户说明书截图。** 使用无客户数据、无真实密钥的演示环境，将截图放入 `06_user_manual_draft.md` 对应位置并排版成正式文档。
6. **补齐软件基本信息。** 把 `05_software_information.md` 中所有【需要开发者确认】替换为真实信息。
7. **进行终审扫描。** 对最终源码 PDF、说明书 PDF、截图和压缩包再次检查 Key、Token、Cookie、个人信息、IP、数据库连接串和客户数据。
8. **核对申请平台当期要求。** 确认页数、每页行数、字体、页眉、签章、身份证明和版本命名后再提交。

本目录是技术整理材料，不替代登记机构的最新要求或专业法律意见。
