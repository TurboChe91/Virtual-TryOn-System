# 穿戴甲AIGC商品视觉生产管理系统软著提交安全及知识产权检查

软件简称：穿戴甲视觉生产系统  
版本号：V1.0

## 1. 审计范围与分级

当前提交审计对象：`../copyright_materials_v2/source_submission_candidate.txt`。`03_source_candidate.txt` 仅作为第一轮原样候选存档；仓库其他区域只做文件名/关键字级排除检查。真实 `.env` 和 `data/` 内容未读取。

分级定义：

- **低风险**：候选中没有真实值，保留通常不导致秘密泄漏；仍需正常人工确认。
- **需要检查**：代码本身可保留，但可能涉及品牌、业务规则、权属、许可证或内部信息，需要开发者/法务作最终判断。
- **必须删除**：一旦出现在最终提交包中必须排除；候选当前未包含时表示继续维持排除。

## 2. 候选文本自动扫描结果

第二轮候选文本共 12,475 个物理行，其中 12,327 行为真实文件内容；相对第一轮仅净化纯叙述性、历史、调试、TODO/生成工具或客户历史说明及相邻空行，未改变函数、变量、类、API、SQL 或业务逻辑。对常见凭证特征扫描结果：

| 特征 | 命中数 |
|---|---:|
| `sk-` 长格式 Key | 0 |
| AWS `AKIA...` Access Key | 0 |
| Google API Key 常见格式 | 0 |
| JWT 三段式令牌 | 0 |
| PEM Private Key 头 | 0 |
| 带用户名密码的数据库 URL | 0 |
| 长格式硬编码 Bearer Token | 0 |

`api_key`、`token`、`secret`、`password`、`cloudflare`、`r2` 等单词有命中，但逐项对应变量名、字段名、脱敏代码、环境变量名、说明文字或对象 key，不是实际运行凭证。

按每页 50 行估算，前 30 页为物理行 1–1500，后 30 页为物理行 10976–12475。两段中 `FINGLOW`、`finglow`、`api.finglow.cn`、`Lunelle Studio` 和 `Lunelle Nails` 的命中数均为 0；详细行号和 `lunelle` 内部技术标识见 `../copyright_materials_v2/brand_scan.md`。

## 3. 分类检查

| 检查项 | 级别 | 候选现状 | 提交前处理 |
|---|---|---|---|
| API Key | 低风险 | 有 `api_key` 字段/变量，无真实值；Key 由环境或 SQLite 运行数据提供 | 保留接口逻辑；最终再次做格式扫描 |
| Token | 低风险 | 有管理令牌和 Cloudflare Token 字段，无真实值；读取接口只返回指纹 | 保留脱敏逻辑；不得附带浏览器存储或 DB 数据 |
| Secret | 低风险 | 主要是脱敏/快照排除关键字，无真实值 | 最终再次扫描 |
| 密码 | 低风险 | 没有用户密码系统；只在 URL 防护中检查 password 参数名 | 可保留 |
| Cookie | 低风险 | 候选没有会话 Cookie 业务；关键字扫描无真实 Cookie | 可保留 |
| 私钥 | 低风险 | 未发现 PEM 私钥或私钥路径 | 可保留 |
| 用户个人信息 | 低风险 | 候选没有真实姓名、手机号、邮箱、证件号或用户记录 | 不得加入真实数据库、日志或截图中的个人信息 |
| 客户商业机密 | 需要检查 | 无客户数据；但 Prompt 规则、试戴存储结构、成本控制和历史迁移注释可能属于内部流程 | 由权利人确认可公开程度；必要时只从候选副本删除非功能性内部注释 |
| 第三方完整源代码 | 低风险 | 未发现 vendored SDK、node_modules 或第三方库源码 | 继续排除依赖目录和打包产物 |
| 开源许可证风险 | 需要检查 | 候选仅 import 开源包，不复制包源码；仓库未发现根 LICENSE/NOTICE | 确认依赖许可证合规和是否需要随产品提供 notices；软著候选不纳入依赖源码 |
| 自动生成代码 | 低风险 | 候选未发现 `generated by`/`do not edit` 标记；迁移和业务文件表现为项目源码 | 开发者确认是否有生成器产物及其权利来源 |
| AI 提示词中的敏感信息 | 需要检查 | 候选包含完整通用 Prompt 模板和逐甲/视图规则，没有运行时客户 Prompt | 权利人确认 Prompt 是否作为商业秘密；不得附带数据库中的实际客户 StyleSpec/identity_text |
| 客户品牌秘密 | 需要检查 | `FINGLOW`、`api.finglow.cn`、`Lunelle Nails` 属于客户项目历史部署标识；`lunelle` / `Lunelle Studio` 为源码内部历史技术标识 | 客户部署标识应避开普通交存前后 30 页；内部技术标识不得擅自改写 |
| 内网地址 | 低风险 | 未发现真实生产 RFC1918 地址；配置含回环/通配绑定地址和私网防护代码 | `127.0.0.1`、`0.0.0.0` 可保留；不得附加生产内网配置 |
| IP 地址 | 低风险 | 候选中的地址为通用回环/监听默认值；无客户服务器公网 IP | 截图与附件中遮盖实际服务器地址（如有） |
| 数据库连接串 | 低风险 | 主库使用本地 Path + sqlite3，无带密码连接串 | 不得附加 `.env`、Cloudflare ID 或运行数据库 |

## 4. 必须删除/必须保持排除的内容

以下内容当前不在候选中，正式申请包也必须保持排除：

1. `.env` 及任何 `.env.*` 真实环境文件。
2. `data/` 下的 SQLite 数据库、备份、输出、上传、导出和日志。
3. 真实 API Key、管理员 Token、Cloudflare Token、Account ID、D1 Database ID、私有 Bucket 信息。
4. 浏览器 localStorage 导出、Cookie、请求抓包、HTTP Authorization 头。
5. 客户上传图片、生成图片原图、客户款式说明、实际逐甲 identity 文本和客户 SKU 数据，除非权利人明确决定作为脱敏截图材料。
6. 任何真实用户/审核者姓名、客户端 IP、服务器 IP、内网域名。
7. 私钥、证书私钥、SSH 配置、数据库连接密码。
8. 第三方 SDK/依赖包源码、虚拟环境、`node_modules`、构建缓存和压缩产物。
9. 测试中使用的假 token/key 字面量也不建议提交，避免审查歧义；测试已整体排除。

## 5. 需要人工检查的具体源码

### 5.1 命名和品牌

- 源码页面、包和日志名称使用内部历史技术标识 `lunelle` / `Lunelle Studio`。
- `export.py` 的 Shopify Vendor `Lunelle Nails` 属于客户适配内容。
- `publish.py` 使用客户项目历史部署域名 `api.finglow.cn`；`cloudflare.py` 包含对应 R2/D1 部署适配，两者均已置于完整候选中部。
- 拟登记名称统一为“穿戴甲AIGC商品视觉生产管理系统”，简称“穿戴甲视觉生产系统”。

必须确认这些名称均由同一权利人合法使用，或者准备书面说明项目内部代号/历史名称与登记名称的关系。若包含他人品牌，应在最终交存副本中依法处理，但不得回改真实业务仓库来制造不存在的历史。

### 5.2 前身项目和代码权属

README 声明项目存在两个前身项目。候选未复制第三方依赖源码，但“前身代码由谁创作、是否为职务/委托、是否含他人代码”不能仅靠当前文件判断。申请人需要核对 Git 历史、合同、劳动关系和贡献者记录。

### 5.3 历史事件注释

第二轮候选已经移除 544 行纯叙述性/历史/调试/TODO/生成工具或客户历史说明及相邻空行，未改动业务代码。仍建议权利人在排版前逐段复核剩余技术注释；如继续净化，只能在候选副本中删除非功能说明，不得删除代码、改变逻辑或用新注释凑行数。

### 5.4 凭证在运行数据库中的保存

源码中 API profile 和 Cloudflare 配置会写入本地 SQLite，读取 API 只返回脱敏信息；但数据库记录本身可能保存可用凭证。该安全设计风险不等于候选源码泄密，因为运行数据库未提交。任何数据库文件都必须排除。

## 6. 第三方与许可证检查

候选使用但不复制以下主要开源依赖的 API：FastAPI、Starlette、Uvicorn、Pydantic、httpx、Pillow、python-dotenv、python-multipart 等。锁定依赖清单不是第三方源码，未进入候选。

当前仓库根目录未发现 LICENSE、COPYING 或 NOTICE。无法仅凭代码断言每个文件的著作权人或全部依赖的分发义务。建议申请人：

1. 核对 Git 作者和贡献者。
2. 核对前身项目、外包/合作/职务开发关系。
3. 保存第三方依赖清单及许可证副本，作为内部合规档案，不混入软著源程序页。
4. 若有从模板、教程或生成工具取得的大段代码，确认授权并在候选中排除非本人部分。

## 7. 风险结论

候选源码当前未发现真实凭证、数据库连接密码、私钥、用户个人信息或第三方完整源码，可以继续用于人工排版。正式提交前仍有三项关键人工风险：名称/品牌一致性、前身项目及贡献权属、内部 Prompt/历史注释是否属于不宜公开的商业信息。
